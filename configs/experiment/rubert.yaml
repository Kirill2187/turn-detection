# @package _global_

defaults:
  - override /data: default
  - override /model: rubert
  - override /train: default

data:
  batch_size: 32
  eval_batch_size: 64

train:
  max_steps: 10000
  warmup_steps: 50

  accelerator: "cuda"
  devices: [0]
  run_name: rubert-run

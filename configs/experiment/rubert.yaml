# @package _global_

defaults:
  - override /model: rubert

data:
  batch_size: 32
  eval_batch_size: 64

train:
  max_steps: 10000
  warmup_steps: 500
  accelerator: cuda
  devices: 1
  run_name: rubert-run

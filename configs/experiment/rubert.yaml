# @package _global_

defaults:
  - override /data: default
  - override /model: rubert
  - override /train: default

data:
  batch_size: 32
  eval_batch_size: 64

train:
  max_epochs: 1
  accelerator: "cuda"
  devices: [0]
  run_name: rubert-run

data_raw_path: data/raw/

hf_repo_id: inkoziev/Conversations
custom_data_folder: custom_data

type: jsonl # jsonl or chat

batch_size: 16
eval_batch_size: 32
num_workers: 8
train_val_split: 0.1

max_lines: 100000
max_val_lines: 10000
